[package]
name = "tscp-protocol"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib", "cdylib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0.18"

[dev-dependencies]
serde_cbor = "0.11"
sha2 = "0.10"
hex = "0.4"
bincode = "1.3.2"
