[package]
name = "tscp-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
hex = "0.4"

tscp-anchor = {
    path = "../../tscp-anchor/crates/tscp-anchor"
}

tscp-kernel = {
    path = "../../tscp-anchor/crates/tscp-kernel"
}
